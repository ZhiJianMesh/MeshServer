[
{
"name":"requests",
"version":"0.1.0",
"type":"rdb",
"versions":[{
  "minVer":"0.0.0",
  "maxVer":"0.1.0",
  "needModify":false,
  "sqls":[
    "create table if not exists requests (
      cid     int not null, -- 公司ID
      at      int not null, -- utc hour
      api     int not null default 0, -- api访问次数
      fail    int not null default 0, -- 失败的api访问次数
      exc     int not null default 0, -- 异常的api访问次数
      service varchar(255) not null,

      primary key(cid, service, at)
    )",
    "create index if not exists idx_requests_at on requests(at)"
  ]
}]
}
]